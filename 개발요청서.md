# ClaudeBridge — 개발요청서 (확정)

> 출처: 대화를 통한 STEP 1~7 질문·답변으로 작성. 모든 핵심 정책이 결정 완료로 이관되어 개발요청서가 완성되었습니다.

## 1. 확인된 사실

- **목적**: 로컬 PC에서 Claude Code로 작업시킬 때, 사용자가 자리에 없어도 웹 대시보드에서 진행 상황(백로그/진행률)을 확인하고, Claude Code가 사용자 입력(선택지 질문/도구 실행 권한 승인)을 기다리며 멈춰 있을 때 웹에서 바로 응답해 로컬 세션이 이어서 진행되도록 하는 원격 제어 콘솔
- **타깃 사용자**: 본인(jhjeong710@gmail.com) 1인 — Claude Code로 여러 프로젝트를 동시/이동 중 관리하는 개발자
- **현재 대안과 불편함**: 로컬 PC 앞에 있어야만 Claude Code의 진행 상황과 대기 중인 질문을 확인·응답할 수 있음. 외출 중이거나 다른 작업 중일 때 Claude가 멈춰서 기다리고 있는지조차 알 방법이 없음
- **핵심 가치**:
  - 어디서든 웹으로 진행 상황(백로그 상태) 확인
  - 막힌 지점(질문/권한 승인)에서 바로 응답해 로컬 세션 이어가기
  - Claude 토큰 사용 현황 확인
  - **다른 프로젝트에도 명령 1개로 적용 가능한 plug-and-play 이식성** — 폴더 복사·수동 콘솔 설정·프로젝트별 의존성 설치 없이, 이미 설치된 도구 하나로 새 프로젝트를 즉시 연결할 수 있어야 함(3장 참고). 이것이 "프레임워크화"의 실질적 정의다.
- **핵심 시나리오**: Claude Code 세션이 AskUserQuestion 또는 Permission 프롬프트로 멈춤 → 로컬 hooks가 상태를 감지해 로컬 에이전트에 전달 → 로컬 에이전트가 Supabase에 상태 기록 + 웹 푸시 알림 발송 → 사용자가 대시보드에서 알림 확인 후 선택지/승인 버튼 클릭 → 로컬 에이전트가 Supabase를 폴링(수초~10초)해 응답 감지 → tmux 세션에 해당 응답을 키 입력으로 주입 → Claude Code 세션이 이어서 진행
- **비목표(하지 않을 일)**: 웹에서 임의 자유 텍스트로 명령을 주입하는 기능(사전 정의된 선택지/승인 버튼만 허용), 다중 사용자 협업, 1차 범위에서의 다중 프로젝트 동시 관리 UI(다만 나중에 쉽게 추가할 수 있도록 설계는 해둠)
- **사용자 = 비용 지불자**: 동일(본인). 예산은 무료/최소 비용 티어

## 2. 결정 완료 (대화를 통해 확정된 정책 — 개발요청서 본문)

### A. 연동 아키텍처
- 로컬 상주 에이전트(Node.js — 기존 `.claude/hooks`·`.claude/tools`와 동일 런타임 재사용)가 Supabase를 주기적으로 폴링(수초~10초 간격)
- Claude Code는 tmux 세션 안에서 실행하고, 로컬 에이전트가 `tmux send-keys`로 사용자의 웹 응답을 해당 세션에 주입
- 로컬 PC는 인바운드 포트를 열지 않음 — 에이전트는 아웃바운드 통신만 수행(webhook 수신 방식 채택 안 함)

### B. 웹에서 처리하는 '막힌 상황'의 범위
- AskUserQuestion 선택지 질문
- 도구 실행 권한(Permission) 승인/거부 프롬프트
- 자유 텍스트 응답은 웹에서 받지 않음 — 사전 정의된 옵션/버튼 클릭만 허용(보안상 임의 명령 주입 경로 차단)

### C. 인증 및 사용자
- 구글 소셜 로그인 + 이메일 화이트리스트(jhjeong710@gmail.com만 허용)
- 단일 사용자 전용 — 별도 권한 등급 없음

### D. 데이터/백엔드
- Supabase(Postgres) — 프로젝트 상태, 질문·답변 기록, 토큰 사용량 로그 저장
- 질문·답변 기록 및 토큰 사용량 데이터는 30일 후 자동 삭제

### E. 토큰 사용량
- Claude Code가 로컬에 남기는 세션 사용량 기록(입출력 토큰)을 로컬 에이전트가 파싱·집계해 Supabase에 적재 → 대시보드에 표시
- 정확한 로컬 로그 파일 위치/포맷은 구현 착수 시 조사해 확정(→ 3장 미결 질문 참고)

### F. 알림
- 웹 푸시 알림(브라우저 Push API) — 막힌 상황 발생 시 즉시 발송
- 이메일/메신저(Slack 등) 연동 없음

### G. 관리 범위 및 확장 설계
- 1차 목표: 이 프로젝트(Day_4_SDC_Project) 하나만 대상으로 동작 검증
- 확장을 고려해 DB 스키마에 처음부터 `project_id`를 포함시켜, 이후 다른 프로젝트를 추가할 때 스키마 변경 없이 "프로젝트 등록"만으로 확장 가능하게 설계
- **이식은 새 클라우드 리소스를 만들지 않는다**: 다른 프로젝트를 추가할 때도 Supabase/Vercel 프로젝트를 새로 만들지 않고, 이미 배포된 동일 백엔드에 API로 프로젝트를 등록한다(수동 콘솔 조작 금지 — H 항목의 "설치 형태" 참고)

### H. 플랫폼 및 기술 스택
- 프런트/백엔드: Next.js, Vercel 배포
- DB: Supabase
- 로컬 에이전트: Node.js
- 인프라 예산: 무료/최소 비용 티어
- **설치 형태(plug-and-play 요구사항의 구현 방식)**: 로컬 에이전트·hooks 로직은 프로젝트마다 파일을 복사하는 방식이 아니라, **전역 설치 CLI 패키지 1개**(`npm i -g claudebridge`, 로컬 PC에 평생 1회만 설치)로 배포한다.
  - 최초 1회: `claudebridge login` — 개인 액세스 토큰 발급받아 `~/.claudebridge/config.json`에 저장(머신당 1회, 프로젝트당 아님)
  - 프로젝트마다: 그 프로젝트 디렉터리에서 `claudebridge init` **명령 1개**만 실행 → project_id 발급 API 호출(백엔드에 프로젝트 등록) + `.claude/settings.json`에 위임형 훅(전역 CLI를 호출하는 한 줄짜리 훅) 자동 삽입 + tmux 세션 생성 및 Claude Code 실행까지 자동 처리
  - 프로젝트별로 별도 `node_modules`나 의존성 설치가 생기지 않는다(전역 CLI 하나를 모든 프로젝트가 공유) — "불필요한 종속성 없음" 요구사항의 구현 방식
  - 마감(J 항목) 안에 전역 CLI화까지 못 끝내면, 최소한 이 설계 원칙(공유 백엔드 + 위임형 훅 + 파일 복사 최소화)은 지키고 남은 자동화는 backlog에 todo로 남긴다

### I. 보안

- **안전 게이트(최우선 필수 선행 조건)**: ClaudeBridge는 어떤 프로젝트에서도 "1단계: 위험 명령어 차단 강제 여부 검증"을 통과하기 전에는 절대 활성화되지 않는다. 순서는 항상 다음과 같이 고정된다:
  1. **1단계 — 위험 명령어 차단 검증**: 대상 프로젝트의 `.claude/settings.json`에 위험 명령어 차단 훅(이 프로젝트의 `block-dangerous-commands.mjs`와 동등한, Bash PreToolUse 훅)이 실제로 등록되어 동작하는지 확인한다.
     - 없거나 비활성 상태면 **즉시 중단**한다 — 임의로 건너뛰거나 경고만 남기고 진행하지 않는다.
     - `claudebridge` CLI가 배포하는 표준 훅 파일이 없을 때는, CLI에 번들된 검증된 사본을 자동 설치하고 설정에 등록한 뒤 재검증한다(plug-and-play를 해치지 않으면서 안전을 보장하는 방법 — 사용자가 임의로 작성한 미검증 훅을 신뢰하는 것이 아니라 CLI 자체가 보증하는 표준 훅만 자동 설치).
  2. **2단계 — 웹 접근 가능 여부 점검**: 1단계를 통과한 뒤에만 tmux 세션 존재/생성, 로컬 에이전트↔Supabase 연결, Claude Code 프로세스 상태 등 "웹에서 실제로 이 세션에 접근해 조작할 수 있는 상태인지"를 점검한다.
  - 이 두 단계는 `claudebridge init` 실행 시뿐 아니라, 로컬 에이전트가 재시작될 때마다 매번 다시 검증한다(운영 중 누군가 설정을 손댔을 가능성 대비 — 1회성 검증이 아니라 상시 전제조건).
  - 특히 **Permission 승인/거부를 웹에서 대신 눌러주는 기능(B 항목)은 1단계 게이트가 살아있는 동안에만 동작**한다 — 위험 명령어 차단이 꺼진 상태로는 원격 승인 자체가 불가능해야 한다.
- 로컬 PC는 인바운드 포트를 열지 않음(에이전트→클라우드 아웃바운드 통신만)
- 웹→로컬로 전달 가능한 신호는 화이트리스트화된 선택지/승인 응답으로 한정(임의 셸 명령이나 자유 텍스트 주입 경로 없음)
- 인증: 구글 OAuth + 이메일 화이트리스트
- 로컬 에이전트↔Supabase 통신에는 서비스 롤 키를 사용하되, 이 키는 로컬 환경변수에만 보관하고 프런트엔드(브라우저)에는 절대 노출하지 않음

### J. 일정
- **마감: 2026-09-18(내일)까지** — 매우 촉박한 일정. 아래 순서로 "얇은 수직 슬라이스"부터 끝까지 동작시킨 뒤 살을 붙이는 순서를 권장:
  1. 로컬 hooks → 에이전트 → Supabase 상태 기록 (읽기 전용 대시보드까지)
  2. 구글 로그인 + 화이트리스트
  3. AskUserQuestion 1건을 웹에서 응답 → tmux 주입까지 end-to-end 성공
  4. Permission 프롬프트 처리로 확장
  5. 웹 푸시 알림
  6. 토큰 사용량 파싱·표시
- 일정상 1~3번이 사실상 핵심 가치 증명이며, 4~6번은 시간이 부족할 경우 후순위로 밀릴 수 있음(진행하며 재조정)

### K. UX/디자인
- 언어: 한국어만
- **디자인 레퍼런스**: 사용자가 제시한 대시보드 이미지(SaaS 관리자 대시보드 스타일) 참고 — 구체적 문구·아이콘·일러스트를 그대로 베끼지 않고, 아래 레이아웃·톤을 이 프로젝트 데이터에 맞게 적용한다:
  - 좌측 고정 사이드바 내비게이션(대시보드/대기 질문·권한/토큰 사용량/설정) + 하단에 "프로젝트 전환" 진입점(향후 다중 프로젝트 확장 대비)
  - 상단에 상태 요약 통계 카드 4개를 색으로 구분(예: 전체 작업=보라, 대기중 질문·권한=주황, 진행중=파랑, 완료=초록) — todo/needs_info-blocked/doing/done 카운트에 대응
  - 백로그 상태 분포는 도넛/링 차트로, 카테고리별 진행률은 바/게이지로 표현
  - "대기 중인 질문·권한" 목록은 레퍼런스의 업커밍 일정 패널처럼 색상 바 + 시간(발생 시각)으로 항목을 구분해 한눈에 급한 것부터 보이게 구성
  - 부드러운 그라디언트 배경 + 화이트 라운드 카드(그림자) + 퍼플 계열 강조색 톤 유지
  - 이 레퍼런스는 스타일 가이드일 뿐이며, 저작권이 있는 원본 이미지·문구·일러스트 자산을 그대로 복제하지 않는다

### L. 프로젝트명
- **ClaudeBridge** — 로컬 Claude Code 세션과 웹을 잇는다는 의미로 명명

## 3. 다른 프로젝트로 이식하는 방법 (plug-and-play — 개발 완료 후 사용 전략)

**설계 원칙**: 이식 과정에 (a) 파일을 여러 개 복사하는 단계, (b) 클라우드 콘솔에서 손으로 설정하는 단계, (c) 프로젝트마다 새 의존성을 설치하는 단계가 있으면 실패다. 아래 두 명령으로 끝나야 한다.

### 최초 1회(머신 전체 기준 — 프로젝트마다 반복하지 않음)
```
npm i -g claudebridge      # 전역 CLI 설치 (프로젝트별 node_modules 없음)
claudebridge login         # 구글 계정으로 로그인 → 개인 액세스 토큰을 ~/.claudebridge/config.json에 저장
```

### 새 프로젝트에 적용할 때마다(이게 전부)
```
cd <새 프로젝트 디렉터리>
claudebridge init
```
`claudebridge init` 한 번이 자동으로 하는 일(항상 이 순서 — I 항목의 안전 게이트가 0순위):
0. **(안전 게이트 1단계)** 위험 명령어 차단 훅이 등록·동작 중인지 검증 → 없으면 CLI 번들 표준 훅을 자동 설치 후 재검증. 실패하면 여기서 중단하고 이후 단계로 진행하지 않는다.
0-1. **(안전 게이트 2단계)** tmux/에이전트/백엔드 연결 등 "웹 접근 가능 상태"를 점검. 실패하면 원인을 출력하고 중단한다.
1. 배포된 백엔드(Vercel/Supabase — 새로 만들지 않고 기존 것에 API 호출)에 `project_id`를 발급받아 등록
2. 그 프로젝트의 `.claude/settings.json`에 위임형 훅을 삽입/병합(훅 본문은 전역 CLI를 호출하는 한 줄뿐 — 로직 파일을 복사하지 않음)
3. `tmux new -s <project_id>` 세션을 만들고 그 안에서 Claude Code를 실행
4. 완료 메시지에 대시보드 URL과 새 프로젝트가 이미 목록에 추가됐음을 표시

이후에는 로그인해둔 동일 계정·화이트리스트로 대시보드에서 프로젝트를 전환해 바로 사용한다. 즉 이식의 전체 사용자 경험은 "명령 2개(최초 1회) + 명령 1개(프로젝트마다)"로 끝나야 하며, 이 흐름이 성립하도록 G/H 항목의 확장 설계(공유 백엔드, project_id 스키마, 전역 CLI 배포 방식)를 지킨다.

## 4. 미결 질문

- Claude Code 로컬 세션이 남기는 사용량 로그의 정확한 파일 위치/포맷 — 구현 착수 시 실제 파일을 조사해 확정 필요 (E 항목)
- `claudebridge init`이 기존에 이미 열려 있는 tmux 세션을 감지해 재사용할지, 항상 새 세션을 만들지는 구현 설계 시 확정 필요 (A 항목)
- 개인 액세스 토큰 발급 방식(단순 랜덤 토큰 vs OAuth device flow 등)은 구현 착수 시 보안·구현 난이도를 함께 검토해 확정 (I 항목)
